-- ════════════════════════════════════════════════════════════
-- إضافة تفاصيل التغييرات (القيمة القديمة والجديدة لكل حقل)
-- نفّذ هذا في Supabase → SQL Editor → Run
-- (الجدول family_activity_log موجود مسبقاً — هذا فقط يضيف عموداً جديداً)
-- ════════════════════════════════════════════════════════════

alter table family_activity_log
  add column if not exists changes jsonb;

-- مثال على الشكل المخزّن في changes عند تعديل:
-- {"phone1": {"old": "0599111222", "new": "0598111222"}, "camp_id": {"old": "...", "new": "..."}}
