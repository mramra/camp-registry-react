# قائمة تتبّع الكود الميت — نبض المخيم
(آخر تحديث: 2026-06-23)

## ✅ مُكتمَل
1. `countPendingRequests()` بـ`lib/db.js` — حُذفت (استُبدلت بمنطق أدق بالداشبورد)
2. `filterMiss === 'complete'` بـ`FamiliesList.jsx` — حُذف المنطق الميت (تجنباً لالتباس لفظي مع فلتر "✅ مكتمل" الجديد لحالة الموافقة)
3. `components/ui/FamilyCard.jsx` — حُذف الملف بالكامل (لم يكن مستخدَماً بأي صفحة)

## بنود مؤجَّلة (من جلسات سابقة، لم تُراجَع بعد)
- مكوّنات UI متكررة (StatBox, AgeBar, Field, ColPicker) عبر صفحات متعددة — تأجيل اتفقنا عليه صراحة لمرحلة "mui" لاحقة منفصلة

## ✅ بنود من الأجندة القديمة، تأكَّد اكتمالها (لم تكن تحتاج عملاً)
- **"DataPage: spinner يحجب الواجهة عند تحميل الإحصائيات"** — فُحص بدقة (2026-06-23): `loadStats()` لا تستخدم `setLoading` المُحجِّبة على الإطلاق. كل الاستخدامات الفعلية لـ`Spinner` المُحجِّب (7 حالات) مرتبطة بعمليات ثقيلة حقيقية (تصدير Excel، نسخ احتياطي، استرجاع، استيراد، حذف شامل) — سلوك صحيح ومتعمَّد. لا تعديل مطلوب.
