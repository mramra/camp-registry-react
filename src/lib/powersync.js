/**
 * powersync.js — معطّل مؤقتاً
 * PowerSync streaming أوقفناه لأن Supabase NANO وصل حدوده
 */

// stub — لن يُستدعى لأن psReady=false دائماً
export function getPowerSync() {
  return null
}

export const psDb = null
